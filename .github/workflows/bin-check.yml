name: bin-check.yml
on:
  push:
    branches:
      - main
    tags:
      - "*"
  pull_request:
  workflow_dispatch:

# least privilege as default, should be overridden per job if necessary
permissions:
  contents: read

jobs:
  dump_data:
    name: Test dump_data.sh
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgis/postgis:16-3.5
        env:
          POSTGRES_HOST_AUTH_METHOD: trust
        ports:
          - 5432:5432
        # needed because the postgres container does not provide a healthcheck
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up backend environment
        uses: maykinmedia/setup-django-backend@4a6c5facc65bb1dd510cf06be20871db11a156b3 # v1.4.2
        with:
          apt-packages: "postgresql-client"
          python-version: "3.12"
          optimize-postgres: "no"
          setup-node: "no"

      - name: Run setup db & dump data
        run: |
          createdb -h localhost -U postgres openproduct
          python src/manage.py migrate
          src/manage.py loaddata demodata
          SCRIPTPATH=bin DUMP_FILE=dump.sql bin/dump_data.sh --combined
          SCRIPTPATH=bin TAR_FILE=dump.tar bin/dump_data.sh --csv
        env:
          DB_PASSWORD: ""
          DB_USER: postgres
          DB_NAME: openproduct
          DB_HOST: localhost
          SECRET_KEY: secret
          DJANGO_SETTINGS_MODULE: openproduct.conf.ci
          ALLOWED_HOSTS: localhost,127.0.0.1

      - name: validate dump
        run: |
          grep "COPY public.producten_product" dump.sql
          grep "COPY public.producttypen_producttype" dump.sql
          grep "COPY public.locaties_locatie" dump.sql
          ! grep "COPY public.auth_group" dump.sql
      - name: load into other db
        run: |
          createdb -h localhost -U postgres test
          psql -v ON_ERROR_STOP=1 -h localhost -U postgres -d test -f dump.sql
      - name: validate csv dump
        run: |
          tar -xf dump.tar
          test -f producten_product.csv || exit 1
          ! test -f auth_group.csv || exit 1
          grep "id,uuid,gepubliceerd,aanmaak_datum,update_datum,start_datum,eind_datum,status,prijs,frequentie,verbruiksobject,dataobject,producttype_id,naam,aanvraag_zaak_url,aanvraag_zaak_urn" producten_product.csv
